---
import Footer from "./Footer";
import type { SiteNavigationItem } from "@crm/site-ui";

interface Props {
  navigation?: SiteNavigationItem[];
}

const { navigation } = Astro.props;
---

<Footer {...(navigation ? { navigation } : {})} />
