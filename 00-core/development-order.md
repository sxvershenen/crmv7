# Порядок разработки

Это human-summary, а не обязательный agent pre-read.

1. CRM design system — завершён, текущая authority в `packages/ui`.
2. CRM frontend — основной scope завершён, authority в `apps/crm`.
3. Backend + CRM integration — основной scope реализован в `apps/api` и `packages/contracts|domain|db`.
4. Phase 4 public site + CMS — активна. Статус и следующий инкремент: `../07-phase-4-cms/README.md`; долговременный порядок/gates: `../07-phase-4-cms/IMPLEMENTATION-ROADMAP.md`.

Не начинать новый трек по старой фазовой схеме. При планировании открывать только релевантный roadmap-раздел.
